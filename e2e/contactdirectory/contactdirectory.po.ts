import { browser, by, element } from 'protractor';
import { Element } from '@angular/compiler';

export class contactDirPage {
    navigateTo() {
        return browser.get('/');
    }

    getContactDirectory() {
        return element(by.)

    }
}